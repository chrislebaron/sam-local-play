{
  "name": "hello",
  "version": "1.0.0",
  "description": "A simple AWS SAM template that specifies a single Lambda function.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.197.0"
  }
}
